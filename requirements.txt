chalice>=1.31.0
boto3>=1.34.0
requests>=2.31.0
# matplotlib is provided by the Klayers public Lambda layer (see .chalice/config.json).
# Don't add it here or the deploy package will exceed Lambda's 50 MB layer limit.
